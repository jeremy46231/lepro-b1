{
  "config": {
    "flow_title": "{name}",
    "step": {
      "confirm": {
        "description": "Set up {name} ({mac})?"
      },
      "user": {
        "data": {
          "address": "Bluetooth device"
        }
      }
    },
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_device%]",
      "no_devices_found": "[%key:common::config_flow::abort::no_devices_found%]",
      "no_mac": "Could not read the bulb's MAC from its advertisement, which is required to derive its key."
    }
  }
}
